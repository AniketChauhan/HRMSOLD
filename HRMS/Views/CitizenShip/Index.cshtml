@model IEnumerable<HRMS.Models.HRMS_EMP_CITIZENSHIP_MS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CitizenShip_Country_NM)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CitizenShip_Country_NM)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CitizenShip_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CitizenShip_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CitizenShip_ID })
            </td>
        </tr>
    }

</table>
